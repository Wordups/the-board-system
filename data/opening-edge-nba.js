// Opening Edge — NBA first-basket model section (#opening/nba).
// PLACEHOLDER. Replaced wholesale by
//   model/opening-edge/scripts/generate-section.ts --league nba
// on the first refresh with an NBA slate (the twice-daily Refresh Opening
// Edge workflow does this automatically once the season opens). Until then
// this file deliberately does NOT assign window.OPENING_EDGE_NBA, so the
// board shows no NBA tab rather than an empty one — and index.html can load
// it unconditionally without a 404.
